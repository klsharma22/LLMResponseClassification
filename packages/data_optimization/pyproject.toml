[project]
name = "data-optimization"
version = "0.1.0"
description = "Cleaning, transformation, and optimization of the dataset ahead of model training"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/data_optimization"]
