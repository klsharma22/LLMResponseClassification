[project]
name = "data-analysis"
version = "0.1.0"
description = "Exploration and profiling of the raw dataset for LLM response classification"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/data_analysis"]
